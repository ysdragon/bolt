---
title: "Server Control"
weight: 25
summary: "Start and stop the server"
---

### startServer()
Start the server (called automatically by braceEnd).

```ring
startServer()  # Usually not needed
```

### $bolt.stop()
Stop the server gracefully.

```ring
$bolt.stop()
```
